import { Router } from 'express'; import { deleteEmployee, getEmployeeById, getEmployees, postEmployee, postEmployeeAccount, profile, putEmployee, putEmployeeAccountPassword, putServices } from '../controllers/employee.controller.js'; import { requireAdmin, requireAuth, requireNonEmployee } from '../middleware/auth.middleware.js';
export const employeeRouter = Router(); employeeRouter.use(requireAuth); employeeRouter.get('/', getEmployees); employeeRouter.use(requireNonEmployee); employeeRouter.get('/:id/profile', profile); employeeRouter.get('/:id', getEmployeeById); employeeRouter.post('/', postEmployee); employeeRouter.put('/:id', putEmployee); employeeRouter.put('/:id/services', putServices); employeeRouter.post('/:id/account', requireAdmin, postEmployeeAccount); employeeRouter.put('/:id/account/password', requireAdmin, putEmployeeAccountPassword); employeeRouter.delete('/:id', deleteEmployee);

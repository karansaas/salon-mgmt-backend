import { Router } from 'express'; import { deleteEmployee, getEmployeeById, getEmployees, postEmployee, profile, putEmployee, putServices } from '../controllers/employee.controller.js'; import { requireAuth } from '../middleware/auth.middleware.js';
export const employeeRouter = Router(); employeeRouter.use(requireAuth); employeeRouter.get('/', getEmployees); employeeRouter.get('/:id/profile', profile); employeeRouter.get('/:id', getEmployeeById); employeeRouter.post('/', postEmployee); employeeRouter.put('/:id', putEmployee); employeeRouter.put('/:id/services', putServices); employeeRouter.delete('/:id', deleteEmployee);

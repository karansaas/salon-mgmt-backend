import { Router } from 'express'; import { overview } from '../controllers/actionCenter.controller.js'; import { requireAuth, requireNonEmployee } from '../middleware/auth.middleware.js'; export const actionCenterRouter = Router(); actionCenterRouter.use(requireAuth, requireNonEmployee); actionCenterRouter.get('/overview', overview);

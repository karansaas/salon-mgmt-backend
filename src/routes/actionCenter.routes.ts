import { Router } from 'express'; import { overview } from '../controllers/actionCenter.controller.js'; import { requireAuth } from '../middleware/auth.middleware.js'; export const actionCenterRouter = Router(); actionCenterRouter.use(requireAuth); actionCenterRouter.get('/overview', overview);

import { Router } from 'express'; import { overview } from '../controllers/actionCenter.controller.js'; import { requireAuth, requireManagementUser } from '../middleware/auth.middleware.js'; export const actionCenterRouter = Router(); actionCenterRouter.use(requireAuth, requireManagementUser); actionCenterRouter.get('/overview', overview);

import { Router } from 'express'; import { overview } from '../controllers/insight.controller.js'; import { requireAuth, requireNonEmployee } from '../middleware/auth.middleware.js'; export const insightRouter = Router(); insightRouter.use(requireAuth, requireNonEmployee); insightRouter.get('/overview', overview);

import { Router } from 'express'; import { overview } from '../controllers/insight.controller.js'; import { requireAuth, requireManagementUser } from '../middleware/auth.middleware.js'; export const insightRouter = Router(); insightRouter.use(requireAuth, requireManagementUser); insightRouter.get('/overview', overview);

import { HydratedDocument, Schema, Types, model } from 'mongoose';
export interface IEmployee { firstName: string; lastName: string; mobileNumber: string; email?: string; gender?: string; designation: string; joiningDate: Date; salary?: number; address?: string; emergencyContact?: string; skills: string[]; assignedServices: Types.ObjectId[]; profilePhoto?: string; notes?: string; isActive: boolean; createdAt: Date; updatedAt: Date; }
export type EmployeeDocument = HydratedDocument<IEmployee>;
const employeeSchema = new Schema<IEmployee>({ firstName: { type: String, required: true, trim: true, maxlength: 100 }, lastName: { type: String, required: true, trim: true, maxlength: 100 }, mobileNumber: { type: String, required: true, unique: true, trim: true, match: /^\+?[1-9]\d{7,14}$/ }, email: { type: String, trim: true, lowercase: true, match: /^\S+@\S+\.\S+$/ }, gender: { type: String, enum: ['Male', 'Female', 'Other'] }, designation: { type: String, required: true, trim: true, maxlength: 100 }, joiningDate: { type: Date, required: true }, salary: { type: Number, min: 0 }, address: { type: String, trim: true, maxlength: 500 }, emergencyContact: { type: String, trim: true, maxlength: 30 }, skills: { type: [String], default: [] }, assignedServices: { type: [Schema.Types.ObjectId], ref: 'Service', default: [] }, profilePhoto: { type: String, trim: true }, notes: { type: String, trim: true, maxlength: 2000 }, isActive: { type: Boolean, default: true } }, { timestamps: true });
export const Employee = model<IEmployee>('Employee', employeeSchema);

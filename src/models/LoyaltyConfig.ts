import { Schema, model } from 'mongoose'; export interface ILoyaltyConfig { spendPerPoint: number; minimumRedemptionPoints: number; redemptionPoints: number; redemptionValue: number; createdAt: Date; updatedAt: Date; }
const schema = new Schema<ILoyaltyConfig>({ spendPerPoint: { type: Number, default: 100, min: 1 }, minimumRedemptionPoints: { type: Number, default: 100, min: 1 }, redemptionPoints: { type: Number, default: 100, min: 1 }, redemptionValue: { type: Number, default: 50, min: 0.01 } }, { timestamps: true }); export const LoyaltyConfig = model<ILoyaltyConfig>('LoyaltyConfig', schema);

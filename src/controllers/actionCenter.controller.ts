import { Request, Response, NextFunction } from 'express'; import { AppError } from '../utils/AppError.js'; import { getActionCenter } from '../services/actionCenter.service.js'; export const overview = async (req: Request, res: Response, next: NextFunction) => { try { res.json(await getActionCenter(typeof req.query.from === 'string' ? req.query.from : undefined, typeof req.query.to === 'string' ? req.query.to : undefined)); } catch (e) { next(e instanceof Error && /date/i.test(e.message) ? new AppError(400, e.message) : e); } };
